%nav#key-selector.mt-5
  .card
    .card-header
      Afficher dans la tonalité
    .card-block
      .nav
        %ul.pagination
          - Key.primaries.each do |key|
            -#%li{:class => ("current" if @key == key)}= link_to_unless @key == key, key, controller.is_a?(VoicingsController) ? [key, @chord, @voicing || :voicings] : [key, @chord]
            %li.page-item{:class => ("active" if (@key == key or (@key.nil? && key.name == "C")))}= link_to key, [key, @chord], class: "page-link"

