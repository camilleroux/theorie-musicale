%nav#key-selector
  .panel.panel-default
    .panel-heading
      %h2.panel-title Afficher dans la tonalité
    .panel-body
      %ul.nav.nav-pills
        - Key.primaries.each do |key|
          -#%li{:class => ("current" if @key == key)}= link_to_unless @key == key, key, controller.is_a?(VoicingsController) ? [key, @chord, @voicing || :voicings] : [key, @chord]
          %li{:class => ("active" if (@key == key or (@key.nil? && key.name == "C")))}= link_to key, [key, @chord]

